node_modules/
.env
.env.local
dist/
*.log
.DS_Store

# Sensitive: your data + logged-in session — never commit/deploy.
# (enrollment-cache.example.json is the committed sample; the real one is ignored)
enrollment-cache.json
udemy-auth.json
udemy-cookies.json
coursera-auth.json
coursera-cookies.json
coursera-*-cache.json
futurelearn-auth.json
futurelearn-discovery.json
go1-auth.json
go1-discovery.json
revenue-cache.json
revenue-discovery.json
caption-cache.json
coupon-cache.json
discover-output.json
.chromium-profile/
.cdp-profile/
transcript-cache.json
last-update.json
server/dashboard.db
server/dashboard.db-wal
server/dashboard.db-shm
switch-to-ngrok.sh
caption-files/
caption-files-*/

# session scratch
CAPTION-RESUME-NOTE.txt

# Bonus-lecture body backups (course content snapshots — data, not code)
server/bonus-lecture-backups/

# Coursera enrollment snapshots (data captures, not code)
server/coursera-enrollment-*.json

# Data exports (revenue reports, DB dumps, course lists) — CONFIDENTIAL.
# This repo is public; importCourseraRevenue.js archives source exports into
# exports/coursera-revenue/, so this must stay ignored.
exports/
server/data.db

# Instructor profile backups — contain personal data (emails, bios). Never commit.
server/coursera-instructor-backups.json
server/_tmp_*

# Thumbnail working files — resized course art and the original-image-URL
# backups. Course data, not code; this repo is public.
server/.thumb-resized/
server/udemy-thumbnail-backups.json
server/coursera-bio-backups.json

# local editor backups
*.bak
*.bak-*
*.bak[0-9]
server/linkedin-auth.json

# raw authenticated page captures
server/snapshots/

# ── FINANCE / ROYALTY — belongs in the private royalty repo, never here ──────
# This repo is PUBLIC. The royalty work (the master title map, the SharePoint
# sync, the revenue importers and their pages) is commercial data and stays out
# of it. None of it has ever been committed; these lines make sure a careless
# "git add -A" cannot change that.
server/titleMapOverrides.json
server/graphClient.js
server/syncMasterRevenue.js
server/discoverRoyaltyFiles.mjs
server/importMasterRevenue.js
server/importSwoRoyalty.js
server/importTitleMap.js
server/buildCourseMap.js
server/batchRevenue.js
server/batchCoverage.js
server/auditBatches.js
server/reconcile.js
server/revenue.html
server/batches.html
server/course-map.html
server/_tmp_share.mjs
# The coupon report joins every coupon to its parent course, batch and SME from
# the royalty tool, so what it writes is commercial even if the code is not.
server/couponReport.mjs
server/couponWorkbook.py
# One-off diagnostics written during investigations; never meant to be kept.
server/_tmp_*.mjs
